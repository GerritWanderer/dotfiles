return {
  "mrjones2014/smart-splits.nvim",
  opts = {
    -- Stop at the outermost pane instead of wrapping to the far side
    -- (herdr backend otherwise defaults at_edge to 'wrap').
    at_edge = "stop",
  },
}
